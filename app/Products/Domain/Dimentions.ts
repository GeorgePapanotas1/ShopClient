export default class Dimensions {
    length: string;
    width: string;
    height: string;

    constructor(length: string, width: string, height: string) {
        this.length = length;
        this.width = width;
        this.height = height;
    }
}
